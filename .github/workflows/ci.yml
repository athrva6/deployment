name: CI
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node: ["18", "20"]
steps:
  - uses: actions/setup-node@v4
    with:
      node-version: "20"
      cache: "npm"
  - uses: actions/upload-artifact@v4
    with:
      name: build
      path: dist/
